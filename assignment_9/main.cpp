#include <iostream>
#include "Car.h"
#include "Person.h"
#include "Meeting.h"
int main() {

    // std::unique_ptr<Car> fullCar{new Car{5}};

    // std::unique_ptr<Car> emptyCar{new Car{0}};

    // std::shared_ptr<Person> p1 {new Person{"p1","p1@ntnu.no", std::move(fullCar)}};

    // std::shared_ptr<Person> p2 {new Person{"p2","p2@ntnu.no"}};

    // std::cout << *p1 << std::endl;
    // std::cout << p1->hasAvailableSeat() << std::endl;
    // std::cout << *p2 << std::endl;
    // p2->setEmail("p2@email.com");
    // std::cout << p2->hasAvailableSeat() << std::endl;

    // std::cout << Campus{0} << std::endl;
    return 0;
}