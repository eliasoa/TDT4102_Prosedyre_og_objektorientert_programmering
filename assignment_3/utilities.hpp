#pragma once
//Function that returns a pseudorandom int with an upper and lower bound
int randomWithLimits(int lowerBound, int upperBound);

//Function that plays the cannonball game
void playTargetPractice( void );